<div class="card">
    <div class="card-header  flex-row justify-space-between">
        <h2>
                <div class="col-8">{{post.title}}</div>
            </a>
        </h2>
        <div class="col-3">posted by {{post.user.username}} on {{post.date_created}}
        </div>
    </div>
    
    <p class="card-body">
        {{post.content}}
    </p>
</div>

<input type="hidden" name="post-id" value="{{post.id}}" />

<div class="card">
    <div class="card-header"></div>

    <form class="card-body" id="comment-form">
        <p class="form-label">Comment:</p>
        <textarea class="form-input" id="comment-body" name="comment-body"></textarea>
        <button type="submit" class="btn btn-primary">Submit:</button>
    </form>

    <div class="comments">
        {{#each post.comments}}
        <div>
            <p class="card-body2">
                {{comment}} - {{user.username}} {{date_created}}
            </p>
        </div>
        {{/each}}
    </div>
</div>

<script src="/js/comment.js"></script>